# Lovely bridge files

Copy `codex_agent.lua` and `lovely.toml` from this directory into your local Lovely mod installation. The bridge writes its observation and command handshake only inside your local Balatro application-data directory; those runtime files are intentionally not part of this repository.
